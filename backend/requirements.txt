# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
# Production process manager for the bare-metal / systemd path (run with a
# single UvicornWorker — see deploy/lumora.service for the scheduler caveat).
gunicorn>=21.2,<24.0

# --- Database / ORM ---
SQLAlchemy>=2.0,<2.1
alembic>=1.13,<2.0
# Postgres driver (production); harmless to install for the SQLite MVP.
psycopg[binary]>=3.1,<4.0

# --- Settings / validation ---
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# --- Scheduling ---
APScheduler>=3.10,<4.0

# --- CLI ---
click>=8.1,<9.0

# --- LLM provider SDKs ---
openai>=1.40
anthropic>=0.34
google-genai>=0.3

# --- HTTP client (provider adapters) ---
httpx>=0.27

# --- Server-rendered dashboard templates ---
jinja2>=3.1,<4.0
